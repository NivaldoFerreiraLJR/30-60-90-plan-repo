# start minikube
minikube start

# open dashboard
minikube dashboard

# create a deploymet
minikube kubectl -- create deployment hello-node --http-port=8080

# service the adeployment to show the app response
minikube service hello-node
